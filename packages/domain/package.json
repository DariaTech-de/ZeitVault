{
  "name": "@zeitvault/domain",
  "version": "0.0.0",
  "private": true,
  "description": "Geteilte Domaenenlogik: versionierte Compliance-/Regel-Engine (ArbZG)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "fast-check": "^4.8.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
